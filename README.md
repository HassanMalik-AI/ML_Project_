## End to end machine learning project

## setup.py: In Python, setup.py is a module used to build and distribute Python packages. It typically contains information about the package, such as its name, version, and dependencies, as well as instructions for building and installing the package. 

# src: folder: A structured directory to keep your main application code separate and enforce clean, production-level imports.

# __init__.py: A file that makes a folder a Python package and controls how its modules are imported

# Data ingestion is the process of collecting and importing data from different sources into a system for storage or analysis.

# A data transformation file in your Python project is a script that converts raw data into a clean, structured format ready for analysis or machine learning.

# components folder → Contains core modules (like data ingestion, transformation, training) that implement the main logic

# utils.py → Reusable helper functions used across the project

# logger.py → Handles logging of events, errors, and debugging info

# exception.py → Defines custom error handling to manage exceptions cleanly

# pipeline folder → Contains pipeline scripts that orchestrate the flow of data through different components

# The sys module in Python provides access to variables and functions that interact directly with the Python interpreter and its runtime environment.

# .isna(): This looks at every single cell in your dataset and returns True if a value is missing, and False if the value is present. it creates a grid of True/False values the same size as your dataset.

# .duplicated(): Checks your dataset row by row and returns True for any row that is an exact identical copy of a previous row, and False otherwise.

# .describe(): Generates a quick statistical summary of your numerical columns, giving you the count, mean, standard deviation, minimum, maximum, and percentiles at a single glance.

#
#
#
#